/**
 * @nebbos/brand — single source of brand truth (ADR-278 AD-3).
 *
 * Default entry: the typed brand tokens. For the runtime CSS layer import
 * `@nebbos/brand/theme.css`; for the logo import `@nebbos/brand/logo`.
 */
export * from "./tokens";
export { default } from "./tokens";
